$(document).ready(function() {

  $(".chosen-select").chosen({
    no_results_text: "No matching manufacturers found",
    placeholder_text_multiple: "Select the most relevant language first, then add others the snippet could apply to as well.",
    width: "100%"
    });

});
